AzureDiagnostics
| where ResourceType == "VAULTS"
| where Category == "AuditEvent"
| where OperationName has "SetSecret"
| project TimeGenerated, OperationName, CallerIPAddress, ResultType, Resource
| order by TimeGenerated desc
